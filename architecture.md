New invoice section detected
→ create app registration
→ create service principal
→ assign billing role Azure subscription creator on invoice section
→ authenticate principal
→ PUT alias (idempotent)
→ poll alias
→ retrieve subscriptionId
→ attach subscription to ALZ path / management group
→ deploy spoke baseline and connect to regional hub